DATE TYPES
- A value in JS is always of a certain type. 
- There are eight basic data types in JS. 
- we can put any type in a variable. A variable can at the moment be a string and then store a number.
- programming languages that allow such things, such as JS, are called "dynamically typed"
+ Number (Infinity, NaN)
+ BigInt
+ String: single quotes, double quotes, blackticks are extended functionality allow us to embeb variables and expressions into a string by wrapping them
in ${...}.

